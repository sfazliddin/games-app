import React from "react";
import ReactDOM from "react-dom";
import "./styles/global.css"; // Import the global style
import App from "./App";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
